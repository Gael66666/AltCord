(() => {
  const prefix = "altcord";
  const storage = { get(key, fallback) { try { const v = localStorage.getItem(`${prefix}:${key}`); return v === null ? fallback : JSON.parse(v); } catch { return fallback; } }, set(key, value) { localStorage.setItem(`${prefix}:${key}`, JSON.stringify(value)); } };
  const styles = new Map(), plugins = new Map(), enabled = new Set(storage.get("enabled", [])), settings = storage.get("settings", {});
  const notify = message => { const n = document.createElement("div"); n.className = "altcord-toast"; n.textContent = message; document.body.append(n); requestAnimationFrame(() => n.classList.add("visible")); setTimeout(() => { n.classList.remove("visible"); setTimeout(() => n.remove(), 180); }, 2200); };
  const api = {
    version: "0.2.0",
    settings: { get(p, k, fallback) { return settings[p]?.[k] ?? fallback; }, set(p, k, v) { settings[p] ??= {}; settings[p][k] = v; storage.set("settings", settings); } },
    styles: { add(id, css) { this.remove(id); const s = document.createElement("style"); s.dataset.altcordStyle = id; s.textContent = css; document.head.append(s); styles.set(id, s); }, remove(id) { styles.get(id)?.remove(); styles.delete(id); } },
    plugins: {
      register(plugin) { if (!plugin?.id || !plugin.start) throw new Error("AltCord plugin requires id and start()"); plugins.set(plugin.id, { category: "Autre", ...plugin, stop: null }); },
      list() { return [...plugins.values()].map(({ id, name, description, category, stop }) => ({ id, name, description, category, enabled: Boolean(stop) })); },
      enable(id) { const p = plugins.get(id); if (!p || p.stop) return; p.stop = p.start(api) || (() => {}); enabled.add(id); storage.set("enabled", [...enabled]); document.dispatchEvent(new Event("altcord:plugins-changed")); notify(`${p.name} activé`); },
      disable(id) { const p = plugins.get(id); if (!p?.stop) return; p.stop(); p.stop = null; enabled.delete(id); storage.set("enabled", [...enabled]); document.dispatchEvent(new Event("altcord:plugins-changed")); notify(`${p.name} désactivé`); },
      toggle(id) { plugins.get(id)?.stop ? this.disable(id) : this.enable(id); }
    },
    ui: { notify, openSettings: () => document.querySelector("#altcord-settings")?.showModal() }
  };
  const html = value => { const e = document.createElement("span"); e.textContent = value; return e.innerHTML; };
  function mountSettings() {
    if (document.querySelector("#altcord-settings")) return;
    const launcher = document.createElement("button"); launcher.id = "altcord-launcher"; launcher.title = "Paramètres AltCord"; launcher.textContent = "◈";
    const dialog = document.createElement("dialog"); dialog.id = "altcord-settings";
    dialog.innerHTML = `<section class="ac-shell"><aside><div class="ac-brand"><span>◈</span><div><b>AltCord</b><small>Paramètres</small></div></div><nav><button class="active" data-page="plugins">Plugins</button><button data-page="themes">Thèmes</button><button data-page="about">À propos</button></nav><footer>v${api.version}<br>Tout reste local.</footer></aside><main><header><div><p class="ac-eyebrow">ALTCORD</p><h2 id="ac-title">Plugins</h2></div><button class="ac-close" aria-label="Fermer">×</button></header><section id="ac-plugins" class="ac-page"><input id="ac-search" type="search" placeholder="Rechercher un plugin"><div id="ac-categories"></div><div id="ac-plugin-list"></div></section><section id="ac-themes" class="ac-page" hidden><p class="ac-copy">Ajoute ton thème CSS. Il reste local et s’applique uniquement dans Discord.</p><label class="ac-css-label">CSS personnel<textarea id="ac-css" placeholder="/* Mon thème */\n:root { --brand-500: #7289da; }"></textarea></label><button id="ac-apply-css" class="ac-primary">Appliquer le CSS</button></section><section id="ac-about" class="ac-page" hidden><h3>AltCord est indépendant</h3><p class="ac-copy">Ce runtime ne demande, ne lit ni n’envoie aucun token Discord.</p><p class="ac-copy">Les plugins sont indépendants et peuvent être activés ou désactivés sans redémarrage.</p></section></main></section>`;
    document.body.append(launcher, dialog); const title = dialog.querySelector("#ac-title"), search = dialog.querySelector("#ac-search"), categories = dialog.querySelector("#ac-categories"), list = dialog.querySelector("#ac-plugin-list"); let category = "Tous";
    function render() { const all = api.plugins.list(), query = search.value.trim().toLowerCase(), visible = all.filter(p => (category === "Tous" || p.category === category) && `${p.name} ${p.description}`.toLowerCase().includes(query)); const groups = ["Tous", ...new Set(all.map(p => p.category))]; categories.innerHTML = groups.map(g => `<button data-category="${html(g)}" class="${g === category ? "selected" : ""}">${html(g)} <span>${g === "Tous" ? all.length : all.filter(p => p.category === g).length}</span></button>`).join(""); list.innerHTML = visible.length ? visible.map(p => `<article class="ac-plugin"><div><span class="ac-category">${html(p.category)}</span><h3>${html(p.name)}</h3><p>${html(p.description)}</p></div><label class="ac-switch"><input type="checkbox" data-plugin="${html(p.id)}" ${p.enabled ? "checked" : ""}><i></i></label></article>`).join("") : `<p class="ac-empty">Aucun plugin ne correspond à cette recherche.</p>`; }
    launcher.onclick = () => { dialog.showModal(); render(); }; dialog.querySelector(".ac-close").onclick = () => dialog.close(); dialog.addEventListener("click", e => { if (e.target === dialog) dialog.close(); });
    dialog.querySelector("nav").onclick = e => { const page = e.target.dataset.page; if (!page) return; dialog.querySelectorAll("nav button").forEach(b => b.classList.toggle("active", b.dataset.page === page)); dialog.querySelectorAll(".ac-page").forEach(s => s.hidden = s.id !== `ac-${page}`); title.textContent = e.target.textContent; if (page === "plugins") render(); };
    search.oninput = render; categories.onclick = e => { const b = e.target.closest("button"); if (b) { category = b.dataset.category; render(); } }; list.onchange = e => { if (e.target.dataset.plugin) api.plugins.toggle(e.target.dataset.plugin); };
    const css = dialog.querySelector("#ac-css"); css.value = api.settings.get("custom-css", "css", ""); dialog.querySelector("#ac-apply-css").onclick = () => { api.settings.set("custom-css", "css", css.value); api.styles.add("custom-css", css.value); if (!plugins.get("custom-css")?.stop) api.plugins.enable("custom-css"); notify("CSS appliqué"); };
    document.addEventListener("altcord:plugins-changed", render);
  }
  window.AltCord = api;
  window.addEventListener("altcord:ready", () => { enabled.forEach(id => api.plugins.enable(id)); mountSettings(); }, { once: true });
  api.styles.add("base", `.altcord-toast{position:fixed;right:20px;bottom:20px;z-index:1000000;padding:10px 14px;border-radius:8px;background:#5865f2;color:#fff;font:600 14px system-ui;opacity:0;transform:translateY(8px);transition:.18s}.altcord-toast.visible{opacity:1;transform:none}#altcord-launcher{position:fixed;right:18px;top:72px;z-index:1000000;width:36px;height:36px;border:0;border-radius:50%;background:#5865f2;color:#fff;font-size:21px;cursor:pointer;box-shadow:0 4px 15px #0006}#altcord-settings{width:min(930px,calc(100vw - 36px));height:min(650px,calc(100vh - 36px));max-height:none;padding:0;border:1px solid #303a59;border-radius:12px;background:#141827;color:#f4f5fb;font:14px system-ui}#altcord-settings::backdrop{background:#000a}.ac-shell{height:100%;display:grid;grid-template-columns:220px 1fr}.ac-shell aside{padding:22px 12px;border-right:1px solid #303a59;background:#101321}.ac-brand{display:flex;gap:10px;align-items:center;padding:0 10px 24px}.ac-brand>span{display:grid;place-items:center;width:31px;height:31px;border-radius:9px;background:#5865f2;font-size:18px}.ac-brand b,.ac-brand small{display:block}.ac-brand small,.ac-shell footer,.ac-copy,.ac-plugin p{color:#a7b0c8}.ac-shell nav{display:grid;gap:4px}.ac-shell nav button{border:0;border-radius:6px;padding:9px 10px;background:transparent;color:#b5bfd8;text-align:left;font:inherit;cursor:pointer}.ac-shell nav button.active,.ac-shell nav button:hover{background:#252c44;color:#fff}.ac-shell footer{position:absolute;bottom:22px;font-size:11px;padding:0 10px}.ac-shell main{padding:28px;overflow:auto}.ac-shell main header{display:flex;justify-content:space-between;align-items:start;margin-bottom:20px}.ac-eyebrow{margin:0;color:#99a8ff;font-size:10px;font-weight:800;letter-spacing:1.3px}.ac-shell h2{margin:2px 0 0;font-size:27px}.ac-close{border:0;background:transparent;color:#c3cae0;font-size:25px;cursor:pointer}.ac-page[hidden]{display:none}#ac-search{width:100%;padding:11px 12px;border:1px solid #38425f;border-radius:7px;background:#0e1120;color:#fff;font:inherit;outline:none}#ac-categories{display:flex;flex-wrap:wrap;gap:7px;margin:14px 0}#ac-categories button{border:1px solid #35405c;border-radius:20px;padding:5px 9px;background:#1b2133;color:#c3cbe0;font:12px system-ui;cursor:pointer}#ac-categories button.selected{border-color:#7181ff;background:#28335c;color:#fff}.ac-plugin{display:flex;justify-content:space-between;gap:20px;padding:15px;border-bottom:1px solid #2b334d}.ac-plugin h3{margin:4px 0;font-size:15px}.ac-plugin p{max-width:550px;margin:0;font-size:12px}.ac-category{color:#aab6ff;font-size:10px;font-weight:800;letter-spacing:.7px}.ac-switch input{display:none}.ac-switch i{display:block;width:38px;height:22px;border-radius:12px;background:#39415a;cursor:pointer}.ac-switch i::after{content:"";display:block;width:16px;height:16px;margin:3px;border-radius:50%;background:#fff;transition:.15s}.ac-switch input:checked+i{background:#5865f2}.ac-switch input:checked+i::after{transform:translateX(16px)}.ac-css-label{display:block;font-weight:700}.ac-css-label textarea{display:block;width:100%;min-height:250px;margin-top:8px;padding:12px;border:1px solid #38425f;border-radius:7px;background:#0e1120;color:#fff;font:12px ui-monospace,monospace;resize:vertical}.ac-primary{margin-top:12px;border:0;border-radius:7px;padding:10px 14px;background:#5865f2;color:#fff;font-weight:700;cursor:pointer}.ac-empty{color:#a7b0c8}@media(max-width:650px){#altcord-settings{width:100vw;height:100vh;border:0;border-radius:0}.ac-shell{grid-template-columns:1fr}.ac-shell aside{height:auto;padding:12px;border-right:0;border-bottom:1px solid #303a59}.ac-brand{padding:0}.ac-shell nav{display:flex;margin-top:10px}.ac-shell footer{display:none}.ac-shell main{padding:18px}}`);
})();
